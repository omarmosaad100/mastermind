/*
    Create function create_game():
        - Takes input from user for # of trials, pattern size, difficulty 
        - Returns game rules object
    Create function play_game(game_rules):
        - Takes game rules object
        - Creates Guesser Object and "The One Who Knows" Object
        - Runs game loop
        - Returns win or lose
 */

 fn main() {
    let x = Box::new(42);



    println!("{}", x);
}



